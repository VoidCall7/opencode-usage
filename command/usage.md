---
description: 生成 token 用量报告并打开浏览器
---
!`node -e "require(require('os').homedir()+'/.config/opencode/plugin/usage-monitor/report.js').main(['--open'])"`

上面是 OpenCode token 用量报告的终端汇总（由 /usage 命令生成），请原样展示给我，不要重新计算。
